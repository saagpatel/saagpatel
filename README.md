# Saagar Patel

Platform engineer focused on DX and AI infrastructure — things developers install, run in CI, and keep.

## Install these

**Audit your MCP servers** — permission risks, prompt injection threats, and schema drift across your entire agent config:
```shell
uvx --from mcp-permission-audit mcp-audit discover
```

**Generate production-ready MCP servers** — plain English → FastMCP 3.x project with security validation before anything hits disk:
```shell
uvx --from fastmcp-builder mcpforge generate "a tool that..." -o my-server
```

**Audit your GitHub portfolio** — completeness scoring, interest scoring, quick wins, Excel workbook:
```shell
uv tool install github-repo-auditor && audit run
```

## What else

- **Platform infra** — personal-ops control plane, ShipKit Tauri shared core, notification hub, bridge-db cross-system sync
- **Developer tooling** — incident workbench, ticket dashboards, network analyzer and decoder, API reverse engineering, portfolio command center
- **Browser-native** — [neural network playground](https://neural-network-playground.vercel.app), interactive Bayesian reasoning essay, orbital mechanics, LLM debate arena, evolution sim
- **iOS** — RF spectrum visualizer, LiDAR room acoustics, seismometer, AR historical photo overlay, tidal simulation

San Francisco, CA
